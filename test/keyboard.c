#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <dirent.h>
#include <locale.h>

int main() {
    // while (1) {
    //     int key = _getch();

    //     printf("Key: %d\n", key);
    //     if (key == 27)
    //         exit(EXIT_SUCCESS);
    // }

    // char pwd[5];
    // printf("Enter pass: ");
    // getpass(pwd, 5);

    // printf("Password: %s\n", pwd);

    // char chars[] = {'a', 'b', 'c'};
    // int count = 0;

    // while (1) {
    //     if (count == 3) {
    //         // printf("\r ");
    //         count = 0;
    //         continue;
    //     }

    //     printf("%c\r", chars[count]);
    //     count++;

    //     _sleep(500);
    // }

    // char key;
    // while (1) {
    //     key = _getch();
    //     printf("Key: %c - %d\n", key, key);

    //     if (key == 27 || key == 3)
    //         break;
    // }

    // printf("Done!\n");
    return 0;
}